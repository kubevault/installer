apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kubevault.fullname" . }}
  labels:
    {{- include "kubevault.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kubevault.fullname" . }}
subjects:
- kind: ServiceAccount
  name: {{ include "kubevault.fullname" . }}
  namespace: {{ .Release.Namespace }}
